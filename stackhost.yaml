user:
  name: harkirat
  github: harkirat970

language: "python"
python_version: "3.11.9"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "ffmpeg"
  start:
    - "python3 -m Emilia"

env:
  - "ENVIRONMENT=production"
