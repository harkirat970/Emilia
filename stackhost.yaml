user:
  name: harkirat
  github: harkirat970

language: "python"
python_version: "3.12.9"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "ffmpeg"
  start:
    - "python3 -m AlexaMusic"

env:
  - "ENVIRONMENT=production"
